---
lifecycle:
  directory: "tests/fixtures/expire_partitions/config_directory"
  idempotent: true
  partition:
    retention:
      interval: "1 month"
  tables:
    - schema: "test"
      name: "measurements"
      output:
        file:
          naming:
            template: "pgpartix_output.sql"
